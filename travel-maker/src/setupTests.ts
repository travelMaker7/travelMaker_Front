// // src/setupTests.ts

// import { server } from "./mocks/server";

// // 서비스 워커를 시작하기 전에 Jest 환경을 설정합니다.
// beforeAll(() => server.listen());

// // 각각의 테스트 이후에 모든 요청 핸들러를 초기 상태로 되돌립니다.
// afterEach(() => server.resetHandlers());

// // 모든 테스트가 끝난 후 서비스 워커를 정리합니다.
// afterAll(() => server.close());
